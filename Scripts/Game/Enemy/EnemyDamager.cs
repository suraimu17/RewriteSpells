using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Game.players;

namespace Game.enemys {
    public class EnemyDamager : MonoBehaviour
    {
        void DealDamage(EnemyStatus enemy, MagicStatus magicDamage)
        {
            //enemy.nowHP -= magicDamage.damage;
        }
    }
}